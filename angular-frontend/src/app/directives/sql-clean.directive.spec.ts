import { SqlCleanDirective } from './sql-clean.directive';

describe('SqlCleanDirective', () => {
  it('should create an instance', () => {
    const directive = new SqlCleanDirective();
    expect(directive).toBeTruthy();
  });
});
